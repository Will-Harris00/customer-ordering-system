#ifndef __VALIDATION__
#define __VALIDATION__ 1

#include <string>
using namespace std;

/*----- function prototypes --------------------------------------------------*/

bool containsOnlyNum(string);
void extractDate(string);
int validateDate(unsigned int, unsigned int, unsigned int);

#endif
